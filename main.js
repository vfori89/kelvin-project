//create variable for kelvin
const kelvin = 0;

//create variable for celsius
let celsius = kelvin - 273;

//calculate the fahrenheit
let fahrenheit = Math.floor(celsius * (9 / 5) + 32);

//print out the sentance with the fahrenheit temperature in the console
console.log(`The temperature is ${fahrenheit} degrees Fahrengeit.`);
